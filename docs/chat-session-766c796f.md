# 766c796f 聊天记录整理

## 会话信息
- **Session ID**: 766c796f-41cf-4338-ab31-ba932eb03105
- **时间**: 2026-07-06
- **版本**: Claude Code v2.1.198
- **模型**: claude-opus-4-8

---

## 聊天摘要

### 第一轮：需求讨论

**用户需求**：
- 做一个教学程序
- 接入大模型，有识图能力、代码判断
- 课后作业、老师功能
- 老师（大模型）会给每个学生分配一个专用的教学模型（改了提示词那种）
- 可以复习 + 笔记

**Claude 的理解**：
- 本质上是「AI 私教平台」
- 核心亮点：老师给每个学生配一个改过提示词的专属模型 = 每个学生有懂他水平的私教

**关键决策**（用户通过问答确认）：
1. **使用场景**：暂时个人学习使用（桌面程序）
2. **代码判断**：两者结合（运行测试用例 + 大模型点评）
3. **技术栈**：让 Claude 推荐
4. **学科方向**：编程 + 通用学科都要
5. **代码运行**：需要真实运行
6. **大模型**：暂时先用国内模型

---

### 第二轮：技术选型确认

**技术方案**：
- **桌面框架**：Python 后端 + pywebview（本地网页界面）
- **大模型**：统一走 OpenAI 兼容接口，按任务路由不同模型
- **存储**：SQLite 本地库
- **沙箱**：Docker 容器运行学生代码（推荐）或本地子进程

**用户补充**：朋友给了小米模型 API
- 地址：`http://43.131.247.174:3000`
- Key：`dummy`（测试 key）
- 模型：mimo-v2.5-pro、mimo-v2.5、mimo-v2-omni（识图）、mimo-v2.5-tts、mimo-v2.5-asr

---

### 第三轮：API 验证

Claude 验证了小米模型网关：
- ✅ 对话跑通（mimo-v2.5-pro，带思维链 reasoning_content）
- ✅ 识图通过（mimo-v2-omni，读出图片中的 `7 × 8 = 56`）
- ✅ TTS/ASR 可用（免费）

---

### 第四轮：搭建阶段 0 脚手架

**已创建的文件**：
- `main.py` - 桌面入口（pywebview）
- `app/config.py` - 全局配置（.env 读取模型路由）
- `app/llm.py` - 统一 MiMo 接入层（chat / chat_stream / vision）
- `app/server.py` - FastAPI 后端（/api/chat 流式接口 + 静态文件托管）
- `web/` - 前端页面

**验证结果**：
- ✅ 后端、流式输出（打字机效果）、思维链、中文都正常
- ✅ Phase 0 完成

---

### 第五轮：UI 设计讨论

**用户观点**：教学需要温馨的 UI，让人放松，不然枯燥文字不容易进入状态

**Claude 的设计方向**：
- 调用 `ui-ux-pro-max` skill 查风格
- **字体**：Varela Round（圆润） + Nunito Sans + 中文圆体
- **配色**：暖奶油 + 陶土橘 + 柔和鼠尾草绿（治愈系）
- 做成可复用的 `design tokens`（`web/tokens.css`）

---

### 第六轮：布局重构

**用户进一步需求**：像 Obsidian 那样的桌面软件布局

**Claude 设计的布局**：
```
┌──┬──────────┬────────────────────────┬─────────┐
│图│ 学生列表  │      工作区(带标签页)      │ 右侧栏  │
│标│ / 笔记树  │      当前:和某学生对话      │ 学生档案 │
│栏│          │                        │ /错题本  │
├──┴──────────┴────────────────────────┴─────────┤
│ 状态栏:模型 mimo-v2.5-pro · 已连接                 │
└──────────────────────────────────────────────────┘
```

- 最左图标栏：对话 / 笔记 / 作业 / 复习 / 设置
- 侧栏：学生列表（点谁就切到跟谁的专属对话）
- 中间工作区：带标签页的主区域
- 右侧栏：学生档案 / 错题本（可折叠）
- 底部状态栏：模型、连接状态

**状态**：重构过程中遇到 API 连接错误（ECONNRESET / 502），会话中断。部分文件已写入但可能不完整。

---

## 当前项目状态

### 已完成
- [x] 项目脚手架（main.py + app/ + web/）
- [x] MiMo 模型接入层（config.py + llm.py）
- [x] FastAPI 后端（server.py）
- [x] 基础聊天功能（流式 SSE）
- [x] 暖色设计令牌（tokens.css）
- [x] 基础聊天 UI

### 进行中（未完成，因 API 中断）
- [ ] 应用外壳布局（Obsidian 风格）
- [ ] 侧栏 + 图标栏 + 状态栏
- [ ] JS 驱动逻辑（标签页切换、学生列表）

### 待做
- [ ] 阶段 1：存储层（SQLite）+ 学生专属家教
- [ ] 阶段 2：代码沙箱执行
- [ ] 阶段 3：作业生成/批改
- [ ] 阶段 4：复习 + 笔记
- [ ] 阶段 5：语音交互（TTS/ASR）

---

## 模型配置

| 任务 | 模型 | 用途 |
|------|------|------|
| chat | mimo-v2.5-pro | 讲解/家教对话 |
| fast | mimo-v2.5 | 快速回复 |
| vision | mimo-v2-omni | 识图（看图/手写作业） |
| tts | mimo-v2.5-tts | 语音合成（免费） |
| asr | mimo-v2.5-asr | 语音识别（免费） |
